/// @description Setup and variables

// Set my state
myState = itemState.idle;
putDownY = 0;
lengthSound = int64(audio_sound_length(alutiiqSound) * 60) -1;
lengthMekSound = int64(audio_sound_length(mekSound) * 60) -1;